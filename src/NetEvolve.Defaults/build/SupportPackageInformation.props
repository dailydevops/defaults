<Project>

    <PropertyGroup Label="NuGet Package Defaults" Condition=" '$(IsTestableProject)' != 'true' ">
        <PackageTags>netevolve;$(PackageTags)</PackageTags>
    </PropertyGroup>

</Project>