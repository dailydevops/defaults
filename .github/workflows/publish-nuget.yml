name: NuGet Package

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

permissions:
  actions: read
  contents: write
  pull-requests: write

concurrency:
  group: publish-nuget
  cancel-in-progress: true

jobs:
  nuget:
    name: Publish
    if: ${{ github.event.workflow_run.conclusion == 'success' && github.actor != 'dependabot[bot]' && github.actor != 'renovate[bot]' }}
    uses: dailydevops/pipelines/.github/workflows/publish-nuget.yml@a5609bae17fb2cecd1675150a7382ceae207e5d3 # 2.5.116
    with:
      workflowName: ${{ github.event.workflow_run.name }}
      artifactPattern: release-packages-*
      environment: NuGet
      runId: ${{ github.event.workflow_run.id }}
    secrets: inherit
